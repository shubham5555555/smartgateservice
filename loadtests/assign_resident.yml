config:
  target: "http://localhost:5050"
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: Assign resident to flat
    flow:
      - post:
          url: "/buildings/{{ buildingId }}/flats/{{ flatNumber }}/assign"
          json:
            residentId: "{{ residentId }}"

